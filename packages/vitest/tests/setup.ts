import { afterAll, afterEach, beforeAll } from 'vitest'

beforeAll(() => {
  // console.log('vitest beforeAll', import.meta.env.VITE_DUDE)
})

afterAll(() => {
  // console.log('vitest afterAll')
})

afterEach(() => {
  // console.log('vitest afterEach')
})
